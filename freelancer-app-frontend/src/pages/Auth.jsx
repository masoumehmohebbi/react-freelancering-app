import AuthContainer from "../features/authentication/AuthContainer";

const Auth = () => {
  return (
    <div className="flex justify-center">
      <AuthContainer />
    </div>
  );
};

export default Auth;
